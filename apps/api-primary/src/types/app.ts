/** Replace with your domain user type. */
export type AppUser = {
  id: string;
  email?: string;
};

/** Replace with your API token principal type. */
export type ApiPrincipal = {
  clientId: string;
};
