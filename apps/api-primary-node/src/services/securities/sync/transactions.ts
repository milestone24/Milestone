export const validateSecurityTransaction = (
  assetId: string,
  securityId: string,
  transactionId: string
) => {
  /**
   * Security transactions should have a way of being validated against live stock prices.
   */
};
